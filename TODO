 - remove direct dependency of Parser on Sigma
 - get renaming right
 - real module system
 - user-defined type functions
 - disallow type-level closures (that is, defining a type in a let that
   depends on a type variable in the context)
 - row types for protocols
 - type inference
