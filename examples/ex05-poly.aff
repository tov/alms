(* Polymorphic version: A Call from A into C *)

let[C] ap : all 'a 'b. ('a -> 'b) -> 'a -> 'b =
  \'a 'b.
    \f: 'a -> 'b.
      \x: 'a.
        f x

let[A] inc : int -> int =
  \y: int. ap[int,int] (\z: int. z + 1) y

in print[int] (inc 5)
