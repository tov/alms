(* A Lying Interface Module (blame iap2) *)

module[C] ap : (int -> int) -> int -> int =
  \f: int -> int.
    \x: int.
      f (f x)    (* f is used twice here, despite what iap2 claims *)

interface iap2 :> (int -o int) -> int -o int = ap

module[A] inc : int -> int =
  \y: int.
    (\g: int -o int.
       iap2 g y)
    (\z: int. add z 1)

in print[int] (inc 5)
