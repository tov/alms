(* A Blameworthy C Module (blame inc2) *)

module[A] ap : (int -o int) -> int -o int =
  \f: int -o int.
    \x: int.
      f x

module[C] inc2 : int -> int =
  \y: int.
    let g = ap (\z: int. add z 1) in
      g (g y)   (* g is used twice here *)

in printInt (inc2 5)
