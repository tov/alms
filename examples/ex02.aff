(* An Ill-Typed A Module (type error) *)

let[A] ap =
  \f: int -o int.
    \x: int.
      f x

let[A] inc2 =
  \y: int.
    let g = ap (\z: int. z + 1) in
      g (g y)   (* g: (int -o int) is used twice here *)

in print[int] (inc2 5)
