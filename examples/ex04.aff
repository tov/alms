(* The C Module, Corrected *)

let[A] ap : (int -o int) -> int -o int =
  \f: int -o int.
    \x: int.
      f x

let[C] inc2 : int -> int =
  \y: int.
    let g = ap (\z: int. z + 1) in
    let h = ap (\z: int. z + 1) in
      h (g y)

in print[int] (inc2 5)
