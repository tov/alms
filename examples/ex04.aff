(* The C Module, Corrected *)

module[A] ap : (int -o int) -> int -o int =
  \f: int -o int.
    \x: int.
      f x

module[C] inc2 : int -> int =
  \y: int.
    let g = ap (\z: int. add z 1) in
    let h = ap (\z: int. add z 1) in
      h (g y)

in printInt (inc2 5)
