(* An Interface Module Intervenes *)

module[C] ap : (int -> int) -> int -> int =
  \f: int -> int.
    \x: int.
      f x

interface iap :> (int -o int) -> int -o int = ap

module[A] inc : int -> int =
  \y: int.
    (\g: int -o int.
       iap g y)
    (\z: int. z + 1)

in print[int] (inc 5)
