(* Pull in the threads lib. *)

module Thread = struct[C]
  local
    open Prim.Thread
  with
    type thread = Prim.Thread.thread
    let threadFork = threadFork
    let threadKill = threadKill
    let threadDelay = threadDelay
    let printThread = printThread
  end
end
